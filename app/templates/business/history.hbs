
<div class="content-padded">
  <h3>Shift History</h3>
  {{#if controllers.business.completedShifts.length}}
    <ul class="table-view">
      {{#each controllers.business.completedShifts}}
        {{partial "business-nonlive-shift"}}
      {{/each}}
    </ul>
  {{else}}
    <p>No shift history.</p>
  {{/if}}
</div>
